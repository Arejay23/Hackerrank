#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main(){
    int n; 
    scanf("%d",&n);
    if(n%2==0)
        {
        if(n>=2)
            {if(n<=5) 
            {
            printf("Not Weird");
        }
             if(n>20)
                 {
                 printf("Not Weird");
             }
    }
    if(n>=6)
        {
        if(n<=20)
            {{printf("Weird");}}
    }
    }
    else
        {printf("Weird");}
    return 0;
}
